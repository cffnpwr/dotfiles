# 運用

日常の操作は[chezmoi](https://www.chezmoi.io/)のコマンドに従う。
本節では本リポジトリ固有の流れを示す。

## 新規環境のセットアップ

chezmoiでこのリポジトリを初期化すると、ブートストラップが順に実行され環境が構築される。
最初に鍵復号のパスフレーズを求められ、以降の復号と各段階の適用が続く。

## 設定の変更

設定はソースを編集して反映する。
ホームを直接編集しない。
パッケージの追加・削除は導入対象のデータを編集する。
変更は`chezmoi apply`で反映され、関連するスクリプトが変化を検知して再実行される。
