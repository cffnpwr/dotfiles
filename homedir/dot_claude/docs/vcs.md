# バージョン管理

VCS操作の方針。

## 基本方針

- VCS操作はjjをデフォルトとする。
- リポジトリルートに `.jj` が無い場合のみgitを使う。
- `.jj` と `.git` の併存（colocated）でもjjを使う。
- 読み取り専用の確認（status・log・diff等）もjjを優先する。
- コマンド変換に迷う場合、VCSリファレンス系のスキルがあれば使う。

## コミットメッセージ

- Conventional Commitで1行で書く。
- gitmojiは任意。
- 1行で表現できない・抽象的になりすぎる場合は、コミットのスコープが過大。
